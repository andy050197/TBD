// Controlador de autenticación (opcional, por ahora solo placeholder)
exports.login = (req, res) => {
  res.status(501).json({ error: 'Autenticación no implementada' });
};

exports.register = (req, res) => {
  res.status(501).json({ error: 'Registro no implementado' });
};